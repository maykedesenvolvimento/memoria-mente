import styled from 'styled-components';
import { colors, sizes } from '../../styles';

export const Container = styled.div`
    font-size: 16px;
    font-weight: 500;
    color: ${colors.red};
    margin-top: ${sizes.spacing*10}px;
    align-self: center;
    justify-self: center;
`;